// time complexity
// O(1),O(logn),O(n),O(nlogn),O(n2),O(n3),O(n!),O(2n),O(n^n)
// 1 sec=10^8
#include<iostream>
#include<vector>
using namespace std;
int main(){

// vector operations
vector<int>v;
v.push_back();
v.pop_back();
v.resize(value);
v[index];
v[index]=value;
v.erase(v.begin()+3);//value

// to print elemets of vector
for(int i=0;i<v.size();i++){
    cout<<v[i]<<"";
}

for(auto i:v){
    cout<<i<<"";
}
}